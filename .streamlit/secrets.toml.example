# Copy this file to .streamlit/secrets.toml locally (never commit the real one),
# or paste its contents into Streamlit Community Cloud's "Secrets" settings
# when deploying.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
